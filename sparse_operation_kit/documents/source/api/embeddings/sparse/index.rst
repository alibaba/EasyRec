SparseOperationKit Sparse Embeddings
====================================
There are several sparse embedding implementations, and all of them 
are equivalent to ``tf.nn.embedding_lookup_sparse``. The supported combiner
is `Mean` and `Sum`.

.. toctree::
   :maxdepth: 2

   Distributed Sparse Embedding <distributed>